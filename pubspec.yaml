name: expandable_listview_example
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_auth: ^1.1.4
  firebase_core: ^1.1.1
  cloud_firestore: ^2.1.0


  cupertino_icons: ^1.0.0

  flushbar: ^1.10.4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true